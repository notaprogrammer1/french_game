[project]
name = "french-phonics-game"
version = "0.4.0"
description = "A terminal game for drilling French sound-spelling chunks and contextual conjugations."
requires-python = ">=3.10"
authors = [{ name = "Paul A" }]
dependencies = []

[project.scripts]
french-phonics = "french_phonics_game.app:main"
